Formuliere die gegebene Antwort ("antwort") auf den Auftrag ("task") als GÜLTIGES JSON-Objekt um, OHNE Inhalte hinzuzufügen oder wegzulassen. Folgt der Auftrag einem Schema (z.B. {"creator": "..."}), halte dich exakt daran, sonst wähle sprechende Felder; in der Antwort fehlende Angaben = null. Antworte mit NUR dem JSON-Objekt, kein Markdown, kein Text davor/danach.
