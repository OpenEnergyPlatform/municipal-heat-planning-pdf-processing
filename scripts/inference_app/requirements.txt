# Standalone requirements for the inference_app (Streamlit RAG chat).
# Install into a dedicated venv — not the root requirements.txt (batch pipeline).

torch==2.6.0
torchvision==0.21.0
transformers==5.3.0
bitsandbytes==0.49.2
accelerate==1.14.0
faiss-cpu==1.14.3
openai==2.44.0
qwen-vl-utils==0.0.14
streamlit==1.58.0
rdflib==7.6.0
pillow
numpy
pymupdf
rapidfuzz
