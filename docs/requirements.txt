# Only what renders the checked-in Markdown. Deliberately not the project's
# own requirements.txt: nothing here imports docpipe, so the docs build needs
# neither torch nor PyMuPDF nor a GPU, and it stays a minute rather than an
# hour.
sphinx==8.1.3
myst-parser==4.0.0
furo==2024.8.6
