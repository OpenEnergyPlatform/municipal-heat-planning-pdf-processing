# docpipe.preprocessing.\_\_main\_\_

`docpipe/preprocessing/__main__.py`, read with `ast` by `scripts/build_docs.py`. The docstrings are the code's own: edit them there, not here.

Allow execution via: python -m docpipe.preprocessing

[Back to the index](../README.md)
