# profiles.scenarios.preprocessing

`profiles/scenarios/preprocessing.py`, read with `ast` by `scripts/build_docs.py`. The docstrings are the code's own: edit them there, not here.

preprocessing.py – What text extraction has to know about English.

Author: Felix Vossel

[Back to the index](../README.md)
