# profiles.scenarios.inference

`profiles/scenarios/inference.py`, read with `ast` by `scripts/build_docs.py`. The docstrings are the code's own: edit them there, not here.

inference.py – What the answer loop says around the ar6 prompts.

English, like the publications and like the answers. See
docpipe/inference/wording.py for what each piece is used for.

Author: Felix Vossel

[Back to the index](../README.md)
