# profiles.scenarios.config

`profiles/scenarios/config.py`, read with `ast` by `scripts/build_docs.py`. The docstrings are the code's own: edit them there, not here.

config.py – Constants for the AR6 profile. The corpus is one JSON index next to
its PDFs, so there is little to configure.

Author: Felix Vossel

[Back to the index](../README.md)
