# docpipe

`docpipe/__init__.py`, read with `ast` by `scripts/build_docs.py`. The docstrings are the code's own: edit them there, not here.

docpipe: generic PDF extraction pipeline. Projects plug in via profiles/.

[Back to the index](../README.md)
