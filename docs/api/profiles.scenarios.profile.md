# profiles.scenarios.profile

`profiles/scenarios/profile.py`, read with `ast` by `scripts/build_docs.py`. The docstrings are the code's own: edit them there, not here.

AR6 scenario literature: the publications the IIASA AR6 scenario database cites.

[Back to the index](../README.md)
