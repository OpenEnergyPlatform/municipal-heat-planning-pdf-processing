# docpipe.chunking.\_\_main\_\_

`docpipe/chunking/__main__.py`, read with `ast` by `scripts/build_docs.py`. The docstrings are the code's own: edit them there, not here.

Allow execution via: python -m docpipe.chunking

[Back to the index](../README.md)
