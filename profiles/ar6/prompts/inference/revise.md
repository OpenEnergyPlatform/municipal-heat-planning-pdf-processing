Correct the given answer ("answer") to the task ("task") using the precise single-image read-offs ("readings") — these come from a focused second check per figure and are more reliable than the values in the previous answer. Replace diverging image values, change nothing else, and keep the marking "read off from the figure". Respond with ONLY a JSON object: {"answer": "<the corrected answer>"}
